{
  "name": "ai-canva",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix functions",
    "test": "npm test --prefix server && npm test --prefix client && npm test --prefix functions",
    "test:watch": "concurrently -n server,client -c blue,green \"npm run test:watch --prefix server\" \"npm run test:watch --prefix client\"",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
